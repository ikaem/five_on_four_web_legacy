export enum APP_API_PATH {
  WEATHER = 'weather',
  MATCHES = 'matches',
  MATCH = 'match',
}

// TODO not sure i will need all of thes

// TODO continue settiung up feature service for weather, so can get data in component for client side rendering

