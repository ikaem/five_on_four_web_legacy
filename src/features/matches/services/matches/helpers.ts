import { Dispatch, SetStateAction } from "react";
import { MatchesServiceDataState } from "./types";

export const getHandlersForMatchesServiceState =<T> (prevState: MatchesServiceDataState<T>, stateSetter: Dispatch<SetStateAction<MatchesServiceDataState<T>>>) => {
// TODO not sure if this will work
  cont 

}